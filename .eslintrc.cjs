module.exports = {
  // Keep the existing .eslintrc.json behavior while providing a config
  // file that ESLint v9 can find when invoked directly.
  extends: ["next/core-web-vitals", "next/typescript"],
  root: true,
};
